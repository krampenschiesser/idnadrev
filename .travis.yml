language: java
jdk:
  - oraclejdk8

install:
  - TERM=dumb ./gradlew --info assemble

script:
  - TERM=dumb ./gradlew --info check

before_install:
  - "export DISPLAY=:99.0"
  - "sh -e /etc/init.d/xvfb start"